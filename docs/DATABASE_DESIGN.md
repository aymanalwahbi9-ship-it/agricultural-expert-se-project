# تصميم قاعدة بيانات نظام الخبير الزراعي

## 1. الغرض من الوثيقة

تحدد هذه الوثيقة التصميم المنطقي لقاعدة بيانات نظام الخبير الزراعي. يربط التصميم بين المحاصيل والحالات الزراعية والأعراض وقواعد التربة والتوصيات وعمليات التشخيص وسجل النتائج.

يخدم التصميم الإصدار التجريدي الحالي، لذلك يدعم نتائج تحليل الصورة وقراءات التربة المحاكاة دون ربط النظام بمستشعر أو نموذج تعلم آلي فعلي.

## 2. مبادئ التصميم

يعتمد التصميم على قاعدة بيانات علائقية. يمثل كل جدول مفهومًا رئيسيًا في النظام، وتستخدم المفاتيح الأساسية لتمييز السجلات، بينما تستخدم المفاتيح الخارجية لربط السجلات المرتبطة.

يجب حفظ مصدر كل قراءة ووقت تسجيلها، كما يجب فصل نتائج الصورة ونتائج التربة عن النتيجة المدمجة حتى يمكن تفسير القرار ومراجعة البيانات التي أدت إليه.

لا تحفظ التوصية داخل نتيجة التشخيص فقط؛ بل ترتبط التوصية بمصدر معرفة موثق، ثم تحفظ النتيجة التي عُرضت للمستخدم وقت تنفيذ العملية.

## 3. الجداول الرئيسية

### 3-1 جدول المحاصيل `crops`

يخزن المحاصيل التي يدعمها النظام، مثل البن والطماطم والبطاط.

| الحقل | النوع المقترح | الوصف |
|---|---|---|
| `id` | BIGINT | المفتاح الأساسي |
| `name_ar` | VARCHAR | اسم المحصول بالعربية |
| `name_en` | VARCHAR NULL | الاسم الإنجليزي عند توفره |
| `description` | TEXT NULL | وصف مختصر للمحصول |
| `is_active` | BOOLEAN | يحدد ظهور المحصول في الواجهة |
| `created_at` | TIMESTAMP | تاريخ الإنشاء |
| `updated_at` | TIMESTAMP | تاريخ آخر تعديل |

### 3-2 جدول الحالات الزراعية `conditions`

يخزن الأمراض والآفات وحالات نقص العناصر التي يمكن أن تظهر في نتائج النظام.

| الحقل | النوع المقترح | الوصف |
|---|---|---|
| `id` | BIGINT | المفتاح الأساسي |
| `name_ar` | VARCHAR | اسم الحالة بالعربية |
| `name_en` | VARCHAR NULL | الاسم الإنجليزي عند توفره |
| `type` | ENUM | مرض أو آفة أو نقص غذائي أو اضطراب تربة |
| `description` | TEXT | وصف الحالة |
| `is_active` | BOOLEAN | حالة الاستخدام في البحث |
| `created_at` | TIMESTAMP | تاريخ الإنشاء |
| `updated_at` | TIMESTAMP | تاريخ آخر تعديل |

### 3-3 جدول ارتباط المحاصيل بالحالات `crop_conditions`

يحدد الحالات المحتملة لكل محصول.

| الحقل | النوع المقترح | الوصف |
|---|---|---|
| `id` | BIGINT | المفتاح الأساسي |
| `crop_id` | BIGINT | مفتاح خارجي إلى `crops` |
| `condition_id` | BIGINT | مفتاح خارجي إلى `conditions` |
| `relevance_note` | TEXT NULL | ملاحظة عن ارتباط الحالة بالمحصول |
| `created_at` | TIMESTAMP | تاريخ الإنشاء |

يمنع هذا الجدول عرض حالة مرتبطة بمحصول مختلف دون تنبيه.

### 3-4 جدول الأعراض `symptoms`

يخزن الأعراض أو الصفات المرئية التي يمكن استخدامها في البحث أو نتيجة تحليل الصورة.

| الحقل | النوع المقترح | الوصف |
|---|---|---|
| `id` | BIGINT | المفتاح الأساسي |
| `name_ar` | VARCHAR | اسم العرض بالعربية |
| `description` | TEXT NULL | شرح العرض |
| `created_at` | TIMESTAMP | تاريخ الإنشاء |
| `updated_at` | TIMESTAMP | تاريخ آخر تعديل |

### 3-5 جدول ارتباط الحالات بالأعراض `condition_symptoms`

يربط الحالات الزراعية بالأعراض المرتبطة بها.

| الحقل | النوع المقترح | الوصف |
|---|---|---|
| `id` | BIGINT | المفتاح الأساسي |
| `condition_id` | BIGINT | مفتاح خارجي إلى `conditions` |
| `symptom_id` | BIGINT | مفتاح خارجي إلى `symptoms` |
| `weight` | DECIMAL(5,2) | وزن العرض في المطابقة |
| `created_at` | TIMESTAMP | تاريخ الإنشاء |

### 3-6 جدول مقاطع المعرفة `knowledge_entries`

يخزن النصوص الزراعية الموثقة التي يستخدمها البحث ومحرك القرار.

| الحقل | النوع المقترح | الوصف |
|---|---|---|
| `id` | BIGINT | المفتاح الأساسي |
| `crop_id` | BIGINT NULL | المحصول المرتبط بالمقطع |
| `condition_id` | BIGINT NULL | الحالة المرتبطة بالمقطع |
| `content_type` | ENUM | أعراض أو إجراء أولي أو توصية أو شرح |
| `title_ar` | VARCHAR | عنوان المقطع |
| `content_ar` | TEXT | النص العربي الموثق |
| `keywords` | JSON | الكلمات المفتاحية والمرادفات |
| `source_name` | VARCHAR | اسم المصدر |
| `source_reference` | VARCHAR NULL | رابط أو مرجع المصدر |
| `reviewed_at` | DATE NULL | تاريخ مراجعة المقطع |
| `is_active` | BOOLEAN | يحدد صلاحية استخدام المقطع |
| `created_at` | TIMESTAMP | تاريخ الإنشاء |
| `updated_at` | TIMESTAMP | تاريخ آخر تعديل |

### 3-7 جدول قواعد التربة `soil_rules`

يخزن النطاقات التي تستخدم لتصنيف عناصر التربة حسب المحصول.

| الحقل | النوع المقترح | الوصف |
|---|---|---|
| `id` | BIGINT | المفتاح الأساسي |
| `crop_id` | BIGINT | المحصول المرتبط بالقاعدة |
| `element` | ENUM | N أو P أو K أو pH أو moisture أو temperature |
| `unit` | VARCHAR | وحدة القياس |
| `low_max` | DECIMAL(10,3) | الحد الأعلى للتصنيف المنخفض |
| `suitable_min` | DECIMAL(10,3) | بداية النطاق المناسب |
| `suitable_max` | DECIMAL(10,3) | نهاية النطاق المناسب |
| `high_min` | DECIMAL(10,3) | بداية التصنيف المرتفع |
| `created_at` | TIMESTAMP | تاريخ الإنشاء |
| `updated_at` | TIMESTAMP | تاريخ آخر تعديل |

### 3-8 جدول التوصيات `recommendations`

يربط الحالة الزراعية بتوصية إرشادية موثقة.

| الحقل | النوع المقترح | الوصف |
|---|---|---|
| `id` | BIGINT | المفتاح الأساسي |
| `condition_id` | BIGINT | الحالة المرتبطة بالتوصية |
| `knowledge_entry_id` | BIGINT | مقطع المعرفة الذي يثبت التوصية |
| `title_ar` | VARCHAR | عنوان التوصية |
| `content_ar` | TEXT | نص التوصية الإرشادية |
| `warning_ar` | TEXT NULL | التنبيه أو حدود الاستخدام |
| `is_active` | BOOLEAN | صلاحية التوصية |
| `created_at` | TIMESTAMP | تاريخ الإنشاء |
| `updated_at` | TIMESTAMP | تاريخ آخر تعديل |

## 4. جداول عمليات التشخيص

### 4-1 جدول حالات التشخيص `diagnosis_cases`

يمثل عملية تشخيص واحدة بدأها المستخدم.

| الحقل | النوع المقترح | الوصف |
|---|---|---|
| `id` | BIGINT | المفتاح الأساسي |
| `crop_id` | BIGINT | المحصول المختار |
| `operation_type` | ENUM | صورة أو تربة أو مدمج أو بحث |
| `status` | ENUM | بدأ أو مكتمل أو غير حاسم أو فشل |
| `started_at` | TIMESTAMP | وقت بدء العملية |
| `completed_at` | TIMESTAMP NULL | وقت اكتمال العملية |
| `created_at` | TIMESTAMP | تاريخ الإنشاء |
| `updated_at` | TIMESTAMP | تاريخ آخر تعديل |

### 4-2 جدول ملاحظات الصورة `image_observations`

يخزن الصورة ونتيجة التحليل الفعلي أو المحاكى.

| الحقل | النوع المقترح | الوصف |
|---|---|---|
| `id` | BIGINT | المفتاح الأساسي |
| `diagnosis_case_id` | BIGINT | عملية التشخيص |
| `file_path` | VARCHAR NULL | مسار الصورة عند حفظها |
| `source` | ENUM | كاميرا أو ملف أو محاكاة |
| `condition_id` | BIGINT NULL | الحالة المحتملة |
| `confidence` | DECIMAL(5,4) NULL | درجة الثقة |
| `visible_features` | JSON NULL | الصفات المرئية |
| `quality_status` | ENUM | صالح أو منخفض أو مرفوض |
| `created_at` | TIMESTAMP | وقت تسجيل النتيجة |

### 4-3 جدول قراءات التربة `soil_readings`

يخزن القيم الخام ومصدر القراءة ووقت تسجيلها.

| الحقل | النوع المقترح | الوصف |
|---|---|---|
| `id` | BIGINT | المفتاح الأساسي |
| `diagnosis_case_id` | BIGINT | عملية التشخيص |
| `source` | ENUM | حساس أو إدخال يدوي أو محاكاة |
| `nitrogen` | DECIMAL(10,3) | قيمة النيتروجين |
| `phosphorus` | DECIMAL(10,3) | قيمة الفوسفور |
| `potassium` | DECIMAL(10,3) | قيمة البوتاسيوم |
| `ph` | DECIMAL(5,2) | درجة الحموضة |
| `moisture` | DECIMAL(10,3) | الرطوبة |
| `temperature` | DECIMAL(10,3) | درجة الحرارة |
| `units` | JSON | وحدات القياس |
| `connection_status` | ENUM | متصل أو غير متصل أو غير منطبق |
| `quality_status` | ENUM | صالح أو منخفض أو مرفوض |
| `recorded_at` | TIMESTAMP | وقت القراءة |
| `created_at` | TIMESTAMP | وقت الحفظ |

### 4-4 جدول تصنيفات عناصر التربة `soil_assessments`

يحفظ تصنيف كل عنصر بعد تطبيق قواعد المحصول.

| الحقل | النوع المقترح | الوصف |
|---|---|---|
| `id` | BIGINT | المفتاح الأساسي |
| `soil_reading_id` | BIGINT | القراءة المرتبطة |
| `element` | VARCHAR | اسم العنصر |
| `value` | DECIMAL(10,3) | القيمة المستخدمة |
| `status` | ENUM | منخفض أو مناسب أو مرتفع |
| `rule_id` | BIGINT NULL | القاعدة المستخدمة |
| `created_at` | TIMESTAMP | تاريخ التصنيف |

### 4-5 جدول نتائج التشخيص `diagnosis_results`

يحفظ النتيجة التي يعرضها النظام للمستخدم.

| الحقل | النوع المقترح | الوصف |
|---|---|---|
| `id` | BIGINT | المفتاح الأساسي |
| `diagnosis_case_id` | BIGINT | عملية التشخيص |
| `condition_id` | BIGINT NULL | الحالة المحتملة |
| `result_type` | ENUM | صورة أو تربة أو معرفة أو مدمج أو غير حاسم |
| `confidence` | DECIMAL(5,4) NULL | درجة الثقة النهائية |
| `status` | ENUM | أولية أو غير حاسمة أو متعارضة |
| `supporting_indicators` | JSON | المؤشرات التي دعمت النتيجة |
| `conflicts` | JSON NULL | التعارضات بين المصادر |
| `explanation_ar` | TEXT | سبب النتيجة |
| `advisory_notice_ar` | TEXT | التنبيه الإرشادي |
| `created_at` | TIMESTAMP | وقت إنشاء النتيجة |

### 4-6 جدول مصادر النتيجة `diagnosis_result_sources`

يربط النتيجة بالمقاطع المعرفية التي دعمتها.

| الحقل | النوع المقترح | الوصف |
|---|---|---|
| `id` | BIGINT | المفتاح الأساسي |
| `diagnosis_result_id` | BIGINT | النتيجة |
| `knowledge_entry_id` | BIGINT | مصدر المعرفة |
| `match_score` | DECIMAL(5,4) NULL | درجة التطابق |
| `created_at` | TIMESTAMP | وقت الربط |

## 5. سجل العمليات والتقارير

### 5-1 جدول سجل العمليات `history_records`

يتيح للمستخدم إعادة فتح العملية أو حذفها. في الإصدار الحالي يمكن أن يكون السجل محليًا دون حساب مستخدم.

| الحقل | النوع المقترح | الوصف |
|---|---|---|
| `id` | BIGINT | المفتاح الأساسي |
| `diagnosis_case_id` | BIGINT NULL | عملية التشخيص المرتبطة |
| `operation_type` | ENUM | صورة أو تربة أو مدمج أو بحث أو تقرير |
| `summary_ar` | TEXT | ملخص ما ظهر للمستخدم |
| `occurred_at` | TIMESTAMP | وقت العملية |
| `deleted_at` | TIMESTAMP NULL | الحذف المنطقي عند الحاجة |
| `created_at` | TIMESTAMP | تاريخ الإنشاء |
| `updated_at` | TIMESTAMP | تاريخ آخر تعديل |

## 6. العلاقات الأساسية

| العلاقة | النوع |
|---|---|
| المحصول مع الحالات الزراعية | متعدد إلى متعدد عبر `crop_conditions` |
| الحالة الزراعية مع الأعراض | متعدد إلى متعدد عبر `condition_symptoms` |
| المحصول مع قواعد التربة | واحد إلى متعدد |
| الحالة الزراعية مع مقاطع المعرفة | واحد إلى متعدد اختياريًا |
| الحالة الزراعية مع التوصيات | واحد إلى متعدد |
| المحصول مع حالات التشخيص | واحد إلى متعدد |
| حالة التشخيص مع ملاحظات الصورة | واحد إلى متعدد |
| حالة التشخيص مع قراءات التربة | واحد إلى متعدد |
| قراءة التربة مع تصنيفات العناصر | واحد إلى متعدد |
| حالة التشخيص مع النتائج | واحد إلى متعدد |
| نتيجة التشخيص مع مصادر المعرفة | متعدد إلى متعدد عبر `diagnosis_result_sources` |
| حالة التشخيص مع سجل العمليات | واحد إلى متعدد |

## 7. قواعد سلامة البيانات

يجب أن يكون كل محصول نشطًا قبل استخدامه في عملية تشخيص جديدة. ويجب أن تشير الحالة الزراعية والتوصية إلى سجلات موجودة ونشطة عند استخدامها في نتيجة جديدة.

يجب حفظ مصدر القراءة في كل سجل تربة، ولا يجوز اعتبار القراءة المحاكاة قراءة حساس فعلية. كما يجب حفظ درجة الثقة كقيمة بين صفر وواحد أو وفق نطاق موحد يحدده التطبيق.

يجب رفض قيم التربة غير الرقمية أو الخارجة عن الحدود التقنية قبل حفظها. ويجب ألا تُحفظ توصية في نتيجة التشخيص إذا لم ترتبط بمقطع معرفة موثق.

## 8. الفهارس المقترحة

ينبغي إنشاء فهارس على `crops.name_ar` و`conditions.name_ar` وحقول الربط `crop_id` و`condition_id`. كما ينبغي إنشاء فهرس مركب على `knowledge_entries.crop_id` و`knowledge_entries.condition_id`، وفهرس على `diagnosis_cases.created_at` و`history_records.occurred_at` لتحسين البحث في السجل.

## 9. قرار الإصدار الحالي

لا يتضمن التصميم جدول مستخدمين أو أدوارًا إدارية؛ لأن الإصدار الحالي يعتمد على مستخدم زراعي واحد وقاعدة معرفة مهيأة مسبقًا. يمكن إضافة هذه الجداول مستقبلًا دون تغيير الجداول الأساسية الخاصة بالتشخيص.

## المراجع

[1]: https://github.com/aymanalwahbi9-ship-it/agricultural-expert-se-project/blob/main/docs/REQUIREMENTS.md "وثيقة متطلبات نظام الخبير الزراعي"
[2]: https://github.com/aymanalwahbi9-ship-it/agricultural-expert-se-project/blob/main/docs/USE_CASE_ANALYSIS.md "وثيقة تحليل حالات استخدام نظام الخبير الزراعي"
[3]: https://github.com/aymanalwahbi9-ship-it/agricultural-expert-se-project/blob/main/docs/SYSTEM_DESIGN.md "وثيقة تصميم نظام الخبير الزراعي"
